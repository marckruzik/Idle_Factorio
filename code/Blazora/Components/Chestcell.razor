@inherits Cellicon
@using BlazorApp.Shared.Models





@code {
    [Parameter]
    public Chest chest { get; set; }
    [Parameter]
    public int number { get; set; }

    /*
    public override void graphical_update ()
    {
        base.graphical_update ();
        
        int before_val = 0;
        int.TryParse(this.text_bottom_right, out before_val);

        int after_val = get_val ();

        if (before_val != after_val)
        {
            set_val (after_val);
            this.need_refresh = true;
        }
        
    }


    public void set_val(int val)
    {
        if (val != 0)
        {
            this.text_bottom_right = $"{val}";
            this.picture_filename = this.chest.picture_filename;
        }
        else
        {
            this.text_bottom_right = "";
            this.picture_filename = "";
        }

    }


    public int get_val ()
    {
        int resource_total = 19;
        int pack_max = 16;

        int resource_previous = pack_max * this.number;

        int resource_amount = resource_total - resource_previous;
        int resource_current = Math.Min (resource_amount, pack_max);

        return resource_current;
    }
    */
}
